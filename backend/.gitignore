# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

node_modules/
.env
dist/
build/
.cache/

# Runtime-generated upload storage (user uploads + dev email previews).
# The folder is kept in git via uploads/.gitkeep; its contents are ignored.
uploads/*
!uploads/.gitkeep

# Server logs written to the repo root (these are .txt, so *.log above misses them)
server_log*.txt

# Generated QA/compliance reports
*.pdf